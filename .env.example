# Firebase Configuration (production defaults)
# IMPORTANT: Copy serviceAccountKey.json from Firebase Console
# DO NOT commit this file to version control

# USE_REAL_FIREBASE: true uses real Firebase Auth/Firestore in production
USE_REAL_FIREBASE=true

# FIREBASE_CREDENTIALS: path to Firebase service account key
FIREBASE_CREDENTIALS=./serviceAccountKey.json

# TESTING: true enables mock token verification when USE_REAL_FIREBASE=false
TESTING=false

# Firebase project id (used by admin scripts and tooling)
FIREBASE_PROJECT_ID=aura-auth-proj
