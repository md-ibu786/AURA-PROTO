# =============================================================================
# E2E Test Environment Variables
# LOCATION: frontend/.env.e2e.example
# =============================================================================
#
# PURPOSE:
#    Template for E2E test environment configuration. Copy this file to
#    frontend/.env.e2e and fill in the actual test credentials.
#
# USAGE:
#    cp .env.e2e.example .env.e2e
#    # Edit .env.e2e with actual credentials
#    VITE_USE_MOCK_AUTH=true npx playwright test
#
# NOTE:
#    These credentials must be pre-created in Firebase Auth.
#    DO NOT commit actual credentials to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Firebase Authentication Test Users
# These users must exist in your Firebase project with the specified roles
# -----------------------------------------------------------------------------

# Admin user credentials (role: admin)
E2E_ADMIN_EMAIL=admin@aura.edu
E2E_ADMIN_PASSWORD=admin123

# Staff user credentials (role: staff, assigned to Computer Science department)
E2E_STAFF_EMAIL=staff@aura.edu
E2E_STAFF_PASSWORD=staff123

# Student user credentials (role: student)
E2E_STUDENT_EMAIL=student@aura.edu
E2E_STUDENT_PASSWORD=student123

# -----------------------------------------------------------------------------
# Firebase Configuration
# -----------------------------------------------------------------------------

# Firebase project ID (for Firebase SDK initialization)
VITE_FIREBASE_PROJECT_ID=aura-2026

# Firebase API key (for Firebase SDK)
VITE_FIREBASE_API_KEY=your-api-key-here

# Firebase Auth domain
VITE_FIREBASE_AUTH_DOMAIN=aura-2026.firebaseapp.com

# Firestore database URL
VITE_FIRESTORE_URL=https://aura-2026.firebaseio.com

# -----------------------------------------------------------------------------
# E2E Test Configuration
# -----------------------------------------------------------------------------

# Set to 'true' to use mock authentication instead of real Firebase
# Mock auth bypasses Firebase and uses localStorage for testing
VITE_USE_MOCK_AUTH=false

# Set to 'true' to enable verbose logging during tests
PLAYWRIGHT_VERBOSE=false

# Base URL for E2E tests (usually localhost during development)
PLAYWRIGHT_BASE_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------

# Backend API URL (if different from frontend proxy)
VITE_API_URL=http://localhost:8001/api

# -----------------------------------------------------------------------------
# Mock Data Configuration (used when VITE_USE_MOCK_AUTH=true)
# -----------------------------------------------------------------------------

# Mock admin user
MOCK_ADMIN_ID=mock-admin-001
MOCK_ADMIN_ROLE=admin

# Mock staff user
MOCK_STAFF_ID=mock-staff-001
MOCK_STAFF_ROLE=staff
MOCK_STAFF_DEPARTMENT=dept-cs

# Mock student user
MOCK_STUDENT_ID=mock-student-001
MOCK_STUDENT_ROLE=student
MOCK_STUDENT_DEPARTMENT=dept-cs

# =============================================================================
# IMPORTANT: Never commit actual credentials!
# =============================================================================
