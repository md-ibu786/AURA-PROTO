---
phase: 08-runtime-hotspot-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/kg/router.py
  - api/tasks/document_processing_tasks.py
  - api/tests/test_kg_lookup_paths.py
autonomous: true
requirements:
  - PERF-01
  - PERF-03

must_haves:
  truths:
    - "KG document status lookup no longer scans the full notes collection to find one document"
    - "KG processing queue only reads actively processing notes instead of iterating every note"
    - "KG request handlers remain thin orchestration paths and do not reintroduce scan-based synchronous fallback loops"
  artifacts:
    - path: "api/kg/router.py"
      provides: "Bounded KG status and queue Firestore queries"
      contains: "FieldFilter"
    - path: "api/tasks/document_processing_tasks.py"
      provides: "Canonical bounded note lookup helper for task status updates"
      contains: "_find_note_by_id"
    - path: "api/tests/test_kg_lookup_paths.py"
      provides: "Regression tests proving request paths avoid full note scans"
      exports: ["test_get_document_kg_status_avoids_full_scan", "test_get_processing_queue_filters_in_firestore"]
  key_links:
    - from: "api/kg/router.py"
      to: "api/tasks/document_processing_tasks.py"
      via: "shared bounded note lookup pattern"
      pattern: "FieldFilter\(\"id\""
    - from: "api/kg/router.py"
      to: "ProcessingQueueItem"
      via: "Firestore query filtered by kg_status"
      pattern: "kg_status"
---

<objective>
Remove the audited Firestore note scans from KG request paths and align task-path note resolution with the same bounded lookup strategy.

Purpose: KG status checks and queue polling currently scale with total note count, which makes common request flows slower over time and keeps async handlers busy with synchronous scan work.

Output: Remediated KG router/task lookup logic and regression tests that prove no full note scan fallback remains in active request paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-runtime-hotspot-remediation/08-RESEARCH.md

# Key source files
@api/kg/router.py
@api/tasks/document_processing_tasks.py
@api/modules/models.py
</context>

<interfaces>
From api/modules/models.py:
```python
class DocumentKGStatus(BaseModel):
    document_id: str
    module_id: str
    file_name: str
    kg_status: KGStatus
    kg_processed_at: Optional[datetime] = None
    kg_error: Optional[str] = None
    chunk_count: Optional[int] = None
    entity_count: Optional[int] = None

class ProcessingQueueItem(BaseModel):
    document_id: str
    module_id: str
    file_name: str
    status: KGStatus
    progress: int
    step: str
    started_at: datetime
```

From api/tasks/document_processing_tasks.py:
```python
def _find_note_by_id(document_id: str, module_id: Optional[str] = None)
def update_document_status(document_id: str, status: str, ..., module_id: Optional[str] = None)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend regression tests for bounded KG lookups</name>
  <files>api/tests/test_kg_lookup_paths.py</files>
  <behavior>
    - Test 1: `get_document_kg_status` resolves a note by bounded Firestore lookup and never falls back to `collection_group('notes').stream()`.
    - Test 2: `get_processing_queue` queries only notes with `kg_status == 'processing'` and maps the returned docs into `ProcessingQueueItem` results.
    - Test 3: `_find_note_by_id` prefers scoped module lookup when `module_id` is provided and uses stored `id` equality query when it is not.
    - Test 4: Missing-index or query failures surface as explicit failure behavior without reintroducing a stream-all-notes fallback.
  </behavior>
  <action>
Create `api/tests/test_kg_lookup_paths.py` with mocked Firestore collection-group/query objects covering the KG router and task helper paths. Assert against the query shape and explicitly fail the test if any code path calls `db.collection_group('notes').stream()` without an equality filter for the target note or queue status.

Test structure requirements:
1. Import `get_document_kg_status` and `get_processing_queue` from `api.kg.router`.
2. Import `_find_note_by_id` from `api.tasks.document_processing_tasks`.
3. Mock `db.collection_group('notes')` and `db.collection_group('modules')` so the tests can assert method calls (`where`, `limit`, `stream`) in order.
4. Cover one successful single-document lookup, one successful processing-queue lookup, one scoped module lookup, and one error path.
  </action>
  <verify>
    <automated>pytest api/tests/test_kg_lookup_paths.py -q</automated>
  </verify>
  <done>New test file exists and fails before implementation because current router paths still use the audited scan behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Replace router scan paths with bounded Firestore queries</name>
  <files>api/kg/router.py</files>
  <action>
Update `api/kg/router.py` so both audited endpoints stop scanning the full `notes` collection group:

1. In `get_document_kg_status(document_id)`, remove the `__name__` range query plus Python fallback list comprehension. Replace it with a bounded lookup strategy that uses the stored `id` field with `limit(1)` and returns 404 when no matching note exists. Do not retain any `collection_group('notes').stream()` fallback.
2. In `get_processing_queue()`, replace the Python-side full scan/filter loop with a Firestore query that requests only `kg_status == 'processing'` documents, then map the returned docs with `_doc_to_queue_item`.
3. If the queue query raises a Firestore/index error, log an actionable message and return an explicit HTTP failure instead of silently degrading back to a full scan.

Keep the existing response contracts (`DocumentKGStatus`, `List[ProcessingQueueItem]`) unchanged.
  </action>
  <verify>
    <automated>pytest api/tests/test_kg_lookup_paths.py::test_get_document_kg_status_avoids_full_scan api/tests/test_kg_lookup_paths.py::test_get_processing_queue_filters_in_firestore -q</automated>
  </verify>
  <done>`api/kg/router.py` no longer contains a stream-all-notes fallback in the audited status or queue handlers, and the new router regression tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Align task-path note lookup with the same bounded strategy</name>
  <files>api/tasks/document_processing_tasks.py</files>
  <action>
Refine `_find_note_by_id()` in `api/tasks/document_processing_tasks.py` so task-path note resolution matches the bounded router strategy:

1. Preserve the existing preferred path: when `module_id` is available, resolve the module once and then perform direct note lookup under `module_ref.collection('notes').document(document_id)`.
2. Keep the module-less fallback bounded by querying the stored `id` field with `limit(1)`.
3. Remove or reject any temptation to add a stream-all-notes fallback here; task status updates must not regress into O(n) note iteration.
4. Ensure logging remains explicit on lookup errors, but do not leak raw Firestore internals into user-facing responses.

This task is the PERF-03 guardrail for the phase: request handlers and their downstream task helpers must remain dispatch/lookup paths, not scan-heavy blocking paths.
  </action>
  <verify>
    <automated>pytest api/tests/test_kg_lookup_paths.py -q</automated>
  </verify>
  <done>Task-path note resolution uses only bounded lookup strategies, and the shared regression file passes end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Firestore | Request handlers depend on Firestore query behavior and index availability |
| API→Celery task helper | Router and task code must resolve the same note identity safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | `api/kg/router.py` request handlers | mitigate | Remove full collection-group note scans and forbid stream-all-notes fallback in active request paths |
| T-08-02 | Tampering | `_find_note_by_id` note resolution | mitigate | Prefer module-scoped direct lookup and use stored `id` equality fallback only when module_id is absent |
| T-08-03 | Information Disclosure | Firestore query/index failures | mitigate | Log actionable backend detail, but raise sanitized HTTP failures instead of leaking raw backend internals to clients |
</threat_model>

<verification>
1. Run `pytest api/tests/test_kg_lookup_paths.py -q`.
2. Verify audited scan fallback is gone from the KG router: `rg "collection_group\(\"notes\"\)\.stream\(" api/kg/router.py api/tasks/document_processing_tasks.py`.
3. Verify the queue path now filters by status in Firestore: `rg "kg_status" api/kg/router.py`.
</verification>

<success_criteria>
- [ ] `get_document_kg_status()` no longer iterates the full notes collection to find one note
- [ ] `get_processing_queue()` queries only processing notes instead of filtering all notes in Python
- [ ] `_find_note_by_id()` stays bounded with scoped module lookup or `id == document_id` equality query
- [ ] `pytest api/tests/test_kg_lookup_paths.py -q` passes
- [ ] No new synchronous scan fallback is introduced in audited KG request/task paths
</success_criteria>

<output>
After completion, create `.planning/phases/08-runtime-hotspot-remediation/08-01-SUMMARY.md`
</output>
