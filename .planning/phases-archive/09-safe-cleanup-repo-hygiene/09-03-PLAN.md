---
phase: 09-safe-cleanup-repo-hygiene
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - README.md
  - SECURITY.md
  - documentations/migration-playbook.md
  - AGENTS.md
  - frontend/CLAUDE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STACK.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
must_haves:
  truths:
    - Repo docs point maintainers to `frontend/e2e/` as the only active E2E stack.
    - Security and migration docs explain local-only credential handling after secret cleanup.
    - Planning/codebase reference docs no longer model deleted artifacts as active project structure.
  artifacts:
    - path: README.md
      provides: Canonical operator setup and test commands
      contains: frontend/e2e
    - path: SECURITY.md
      provides: Secret-handling and rotation guidance
      contains: never committed
    - path: .planning/codebase/TESTING.md
      provides: Updated testing surface map
      contains: frontend/e2e
  key_links:
    - from: README.md
      to: frontend/package.json
      via: documented Playwright commands
      pattern: npm run test:e2e
    - from: SECURITY.md
      to: .gitleaks.toml
      via: secret-handling guidance
      pattern: gitleaks
---

<objective>
Update operator docs and planning maps so the cleaned repo state becomes the new source of truth instead of leaving stale instructions behind.

Purpose: Cleanup is incomplete if docs still tell maintainers to use deleted stacks or unsafe credential practices.
Output: Updated runtime/test/security docs plus refreshed planning-map references.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-safe-cleanup-repo-hygiene/09-RESEARCH.md
@.planning/phases/06-verification-recovery/06-05-SUMMARY.md
@.planning/phases/09-safe-cleanup-repo-hygiene/09-01-PLAN.md
@.planning/phases/09-safe-cleanup-repo-hygiene/09-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh operator-facing docs for canonical E2E and safe credentials</name>
  <files>README.md, SECURITY.md, documentations/migration-playbook.md, AGENTS.md, frontend/CLAUDE.md</files>
  <read_first>README.md, SECURITY.md, documentations/migration-playbook.md, AGENTS.md, frontend/CLAUDE.md, frontend/package.json, frontend/playwright.config.ts, e2e/DEPRECATED.md</read_first>
  <acceptance_criteria>
    - `README.md` and `frontend/CLAUDE.md` document E2E execution from `frontend/` / `frontend/e2e`, not `e2e/` as an active stack.
    - `SECURITY.md` and `documentations/migration-playbook.md` explicitly state that service-account keys must remain local-only and be rotated if exposed.
    - `AGENTS.md` stays aligned with `frontend/e2e/` as the canonical stack.
  </acceptance_criteria>
  <action>Update the human-facing docs to match the cleaned repo. In `README.md` and `frontend/CLAUDE.md`, replace root `e2e/` run instructions with the active `frontend` Playwright commands from `frontend/package.json`, and update any project-structure text that still treats root `e2e/` as active. In `SECURITY.md` and `documentations/migration-playbook.md`, add explicit remediation language: service-account JSON keys must never be committed, exposed keys must be rotated/disabled, and any replacement credentials must live only in ignored local paths. Keep `AGENTS.md` consistent with the same canonical E2E guidance.</action>
  <verify>
    <automated>rg -n "frontend/e2e|npm run test:e2e|rotate|never committed|local-only" README.md SECURITY.md documentations/migration-playbook.md AGENTS.md frontend/CLAUDE.md; if ($LASTEXITCODE -ne 0) { exit 1 }; $bad = rg -n "cd e2e|npm test|e2e/tests/|root-level `e2e/` directory is deprecated" README.md frontend/CLAUDE.md 2>$null; if ($LASTEXITCODE -eq 0) { Write-Error "Stale root e2e instructions remain`n$bad"; exit 1 }</automated>
  </verify>
  <done>Maintainer-facing docs describe only the active test stack and safe credential handling.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh planning/codebase maps so deleted artifacts are no longer modeled as active</name>
  <files>.planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md</files>
  <read_first>.planning/codebase/STRUCTURE.md, .planning/codebase/TESTING.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md, README.md, AGENTS.md</read_first>
  <acceptance_criteria>
    - `.planning/codebase/TESTING.md` no longer lists root `e2e/` as an active standalone E2E stack.
    - `.planning/codebase/STRUCTURE.md`, `.planning/codebase/ARCHITECTURE.md`, and `.planning/codebase/STACK.md` stop describing deleted root `e2e/` implementation files as active project assets.
    - The codebase docs still describe `frontend/e2e/` and current frontend Playwright tooling correctly.
  </acceptance_criteria>
  <action>Update the generated planning reference docs so future planning does not resurrect deleted cleanup targets. Remove or rewrite sections that currently describe `e2e/package.json`, `e2e/playwright.config.ts`, `e2e/tests/*.spec.ts`, or the root stack as an active testing surface. Replace those references with the canonical `frontend/e2e/` and `frontend/package.json` Playwright workflow. Keep the docs accurate to the cleaned repository state, not the pre-Phase-09 snapshot.</action>
  <verify>
    <automated>$bad = rg -n "E2E \(Standalone\)|e2e/package.json|e2e/playwright.config.ts|e2e/tests/\*\.spec\.ts|Used by: Uvicorn entrypoint and E2E tests in `e2e/playwright.config.ts`" .planning/codebase/STRUCTURE.md .planning/codebase/TESTING.md .planning/codebase/ARCHITECTURE.md .planning/codebase/STACK.md 2>$null; if ($LASTEXITCODE -eq 0) { Write-Error "Stale planning-map references remain`n$bad"; exit 1 }; rg -n "frontend/e2e|frontend/package.json|Playwright" .planning/codebase/STRUCTURE.md .planning/codebase/TESTING.md .planning/codebase/ARCHITECTURE.md .planning/codebase/STACK.md; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>Planning/codebase reference docs model the cleaned repo accurately and point future planners to the active stack only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs->operators | Stale instructions can push maintainers back onto deleted stacks or unsafe credential habits |
| planning-map->future agents | Outdated generated docs can cause future plans to target removed files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | T | README / CLAUDE / AGENTS guidance | mitigate | Rewrite docs to point only to `frontend/e2e/` and local-only credential handling |
| T-09-10 | R | cleanup evidence trail | mitigate | Update docs with explicit rotation/remediation language rather than silent deletion only |
| T-09-11 | I | security guidance drift | mitigate | Add never-commit and rotate-on-exposure instructions in `SECURITY.md` and migration docs |
| T-09-12 | E | future planning against deleted paths | mitigate | Refresh planning/codebase docs so future agents stop targeting removed root `e2e/` assets |
</threat_model>

<verification>
- Grep operator docs for `frontend/e2e` and credential-rotation language.
- Grep planning-map docs to confirm stale root `e2e/` implementation references are removed.
</verification>

<success_criteria>
- Human-facing docs match the cleaned repo state.
- Security/migration docs explicitly describe local-only credential handling and rotation after exposure.
- Planning/codebase references no longer describe deleted root E2E files as active.
</success_criteria>

<output>
After completion, create `.planning/phases/09-safe-cleanup-repo-hygiene/09-03-SUMMARY.md`
</output>
