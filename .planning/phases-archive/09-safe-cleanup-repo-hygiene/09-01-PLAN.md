---
phase: 09-safe-cleanup-repo-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - serviceAccountKey-auth.json
  - serviceAccountKey-old.json
  - config.json
  - .gitleaks.toml
  - .github/workflows/gitleaks.yml
autonomous: false
requirements:
  - CLEAN-02
user_setup:
  - service: google-cloud
    why: Exposed service-account keys must be rotated or disabled after removal from git.
    env_vars:
      - name: GOOGLE_APPLICATION_CREDENTIALS
        source: Local untracked credential file path after key rotation/reissue
      - name: FIREBASE_CREDENTIALS
        source: Local untracked Firebase service account path if tooling still uses it
    dashboard_config:
      - task: Rotate or disable the exposed service accounts represented by the removed JSON files and create replacement local-only credentials if still needed.
        location: Google Cloud Console -> IAM & Admin -> Service Accounts

must_haves:
  truths:
    - Committed private-key JSON files are absent from the tracked repository.
    - Repo hygiene now includes an automated secret-scan guardrail for future changes.
    - Human follow-up required to rotate exposed credentials is explicit instead of implied.
  artifacts:
    - path: .gitleaks.toml
      provides: Repo-level secret scanning configuration
      contains: redact
    - path: .github/workflows/gitleaks.yml
      provides: CI secret scanning workflow
      contains: gitleaks/gitleaks-action
  key_links:
    - from: .github/workflows/gitleaks.yml
      to: .gitleaks.toml
      via: config-path
      pattern: config-path
---

<objective>
Remove committed secret-like credential files and add a repo-level secret scanning guardrail so checkout and review no longer expose private keys.

Purpose: CLEAN-02 requires immediate remediation of the audit’s secret-like residue, not just documentation.
Output: Removed tracked credential blobs, a committed gitleaks config, a CI workflow, and an explicit credential-rotation checkpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-safe-cleanup-repo-hygiene/09-RESEARCH.md
@.gitignore
@SECURITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tracked private-key credential files from the repository</name>
  <files>serviceAccountKey-auth.json, serviceAccountKey-old.json, config.json</files>
  <read_first>.gitignore, serviceAccountKey-auth.json, serviceAccountKey-old.json, config.json, SECURITY.md</read_first>
  <acceptance_criteria>
    - `git ls-files "serviceAccountKey-auth.json" "serviceAccountKey-old.json" "config.json"` returns no paths.
    - The three deleted files no longer exist in the working tree.
    - No replacement credential JSON is added back under another tracked filename in this task.
  </acceptance_criteria>
  <action>Delete `serviceAccountKey-auth.json`, `serviceAccountKey-old.json`, and `config.json` from the repository because all three contain committed `private_key` material. Do not replace them with sanitized committed copies. Preserve the existing local-file convention by leaving `.gitignore` patterns intact; the repo should contain no tracked service-account JSON after this task.</action>
  <verify>
    <automated>$tracked = git ls-files "serviceAccountKey-auth.json" "serviceAccountKey-old.json" "config.json"; if ($tracked) { Write-Error "Tracked credential files remain`n$tracked"; exit 1 }; if ((Test-Path "serviceAccountKey-auth.json") -or (Test-Path "serviceAccountKey-old.json") -or (Test-Path "config.json")) { Write-Error "Credential files still exist in working tree"; exit 1 }</automated>
  </verify>
  <done>Tracked secret-bearing JSON files are removed from git and from the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: Add a repo-enforced gitleaks guardrail</name>
  <files>.gitleaks.toml, .github/workflows/gitleaks.yml</files>
  <read_first>.planning/phases/09-safe-cleanup-repo-hygiene/09-RESEARCH.md, SECURITY.md, .github/workflows/firestore-rules.yml</read_first>
  <acceptance_criteria>
    - `.gitleaks.toml` exists and contains a redaction setting plus at least one allowlist/comment explaining local credential conventions.
    - `.github/workflows/gitleaks.yml` exists and contains `uses: gitleaks/gitleaks-action@v2`.
    - The workflow config references `.gitleaks.toml` via `config-path:`.
  </acceptance_criteria>
  <action>Create `.gitleaks.toml` with repo-specific secret-scan configuration suitable for this codebase, including redacted reporting and any narrowly-scoped allowlist comments only if truly necessary. Create `.github/workflows/gitleaks.yml` that checks out full history, runs `gitleaks/gitleaks-action@v2`, uses the committed config via `config-path: .gitleaks.toml`, emits a SARIF or JSON report artifact, and fails on findings. Do not add broad exclusions for the deleted credential filenames; the workflow should guard against regressions, not silence them.</action>
  <verify>
    <automated>if (!(Test-Path ".gitleaks.toml")) { Write-Error ".gitleaks.toml missing"; exit 1 }; if (!(Test-Path ".github/workflows/gitleaks.yml")) { Write-Error "gitleaks workflow missing"; exit 1 }; rg -n "gitleaks/gitleaks-action@v2|config-path: \.gitleaks\.toml|report-format" ".github/workflows/gitleaks.yml"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>The repo contains a committed secret-scanning config and CI workflow that will catch future credential leaks.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Rotate or disable the exposed Google Cloud service accounts</name>
  <files>SECURITY.md</files>
  <read_first>SECURITY.md, .gitleaks.toml, .github/workflows/gitleaks.yml</read_first>
  <acceptance_criteria>
    - User confirms the service accounts represented by the removed JSON files were rotated, disabled, or replaced.
    - User has a new local-only credential path if the project still needs service-account access.
  </acceptance_criteria>
  <action>Use the committed cleanup as the trigger for mandatory cloud-side remediation: rotate or disable the exposed service-account keys represented by the deleted JSON files, then keep any replacement credentials only in ignored local paths referenced by environment variables. Do not commit new JSON keys back into the repository.</action>
  <what-built>Committed private-key files are gone and a CI guardrail exists, but cloud-side credential rotation cannot be automated safely from the repo.</what-built>
  <how-to-verify>
    1. Open Google Cloud Console → IAM & Admin → Service Accounts.
    2. Find the service accounts represented by the removed files.
    3. Rotate or disable the exposed keys.
    4. If local development still needs them, generate new keys and keep them outside git using the existing ignored filename/path convention.
  </how-to-verify>
  <verify>
    <automated>Write-Output "Manual checkpoint — wait for user confirmation that exposed credentials were rotated or disabled."</automated>
  </verify>
  <done>Cloud-side credential rotation or disablement is acknowledged by the user, and any replacement keys remain local-only.</done>
  <resume-signal>Type "done" after rotation/reissue is complete, or describe any blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo->reviewer workstation | Sensitive key material becomes visible to anyone cloning or reviewing the repo |
| repo->CI | Future commits may reintroduce secrets unless CI scans them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | I | `serviceAccountKey-auth.json`, `serviceAccountKey-old.json`, `config.json` | mitigate | Delete tracked credential files and require human key rotation after removal |
| T-09-02 | T | `.github/workflows/gitleaks.yml` | mitigate | Add CI scanning with committed config so future secret-bearing commits fail early |
| T-09-03 | R | credential remediation process | mitigate | Record explicit human rotation checkpoint so cleanup is not treated as complete before cloud-side remediation |
| T-09-04 | D | overly broad secret-scan exclusions | accept | Keep allowlists narrow and documented; avoid suppressing deleted credential paths |
</threat_model>

<verification>
- Prove the three secret-bearing JSON files are no longer tracked.
- Prove a committed Gitleaks workflow and config exist.
- Wait for the user to confirm cloud-side credential rotation or disablement.
</verification>

<success_criteria>
- The repo no longer contains tracked private-key JSON files.
- Secret scanning is enforced through a committed workflow/config pair.
- Human-required credential rotation is called out and completed before phase closure.
</success_criteria>

<output>
After completion, create `.planning/phases/09-safe-cleanup-repo-hygiene/09-01-SUMMARY.md`
</output>
