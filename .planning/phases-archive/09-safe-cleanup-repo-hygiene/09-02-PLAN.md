---
phase: 09-safe-cleanup-repo-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/coverage/
  - firestore-debug.log
  - frontend/firestore-debug.log
  - e2e/playwright-report/
  - e2e/test-results/.last-run.json
  - e2e/package.json
  - e2e/package-lock.json
  - e2e/playwright.config.ts
  - e2e/run-tests.sh
  - e2e/README.md
  - e2e/data/hierarchy.json
  - e2e/page-objects/ApiHelper.ts
  - e2e/page-objects/ExplorerPage.ts
  - e2e/page-objects/index.ts
  - e2e/tests/api.spec.ts
  - e2e/tests/audio.spec.ts
  - e2e/tests/explorer.spec.ts
  - e2e/DEPRECATED.md
  - .gitignore
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
must_haves:
  truths:
    - Generated reports and logs are no longer tracked as source files.
    - The deprecated root `e2e/` stack no longer contains executable duplicate test code.
    - Ignore rules block the cleaned artifacts from being recommitted accidentally.
  artifacts:
    - path: e2e/DEPRECATED.md
      provides: Tombstone for the retired root E2E stack
      contains: frontend/e2e/
    - path: .gitignore
      provides: Ignore rules for generated reports and logs
      contains: frontend/coverage/
  key_links:
    - from: .gitignore
      to: frontend/coverage/
      via: ignore pattern
      pattern: frontend/coverage/
    - from: e2e/DEPRECATED.md
      to: frontend/e2e/
      via: canonical-stack note
      pattern: frontend/e2e/
---

<objective>
Purge tracked generated artifacts and remove the deprecated root E2E implementation so stale repo artifacts stop competing with the canonical test surface.

Purpose: CLEAN-01 and CLEAN-03 both require safe removal of high-confidence dead code and stale test artifacts now that `frontend/e2e/` is established.
Output: Deleted generated outputs, retired root `e2e/` implementation files, hardened ignore rules, and a retained tombstone note.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-safe-cleanup-repo-hygiene/09-RESEARCH.md
@.planning/phases/06-verification-recovery/06-05-SUMMARY.md
@e2e/DEPRECATED.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete tracked generated reports, logs, and test-result caches</name>
  <files>frontend/coverage/, firestore-debug.log, frontend/firestore-debug.log, e2e/playwright-report/, e2e/test-results/.last-run.json</files>
  <read_first>.gitignore, .planning/phases/09-safe-cleanup-repo-hygiene/09-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `git ls-files "frontend/coverage/**" "firestore-debug.log" "frontend/firestore-debug.log" "e2e/playwright-report/**" "e2e/test-results/.last-run.json"` returns no paths.
    - The removed paths are absent from the working tree.
  </acceptance_criteria>
  <action>Delete the tracked generated artifact trees and files identified by research: `frontend/coverage/`, `firestore-debug.log`, `frontend/firestore-debug.log`, `e2e/playwright-report/`, and `e2e/test-results/.last-run.json`. Treat them as generated outputs only; do not replace them with committed placeholders or keep partial remnants.</action>
  <verify>
    <automated>$tracked = git ls-files "frontend/coverage/**" "firestore-debug.log" "frontend/firestore-debug.log" "e2e/playwright-report/**" "e2e/test-results/.last-run.json"; if ($tracked) { Write-Error "Generated artifacts still tracked`n$tracked"; exit 1 }; foreach ($path in @("frontend/coverage","firestore-debug.log","frontend/firestore-debug.log","e2e/playwright-report","e2e/test-results/.last-run.json")) { if (Test-Path $path) { Write-Error "Artifact still exists: $path"; exit 1 } }</automated>
  </verify>
  <done>Generated logs, coverage output, and stale test-result artifacts are removed from git and disk.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the deprecated root E2E implementation but keep a tombstone</name>
  <files>e2e/package.json, e2e/package-lock.json, e2e/playwright.config.ts, e2e/run-tests.sh, e2e/README.md, e2e/data/hierarchy.json, e2e/page-objects/ApiHelper.ts, e2e/page-objects/ExplorerPage.ts, e2e/page-objects/index.ts, e2e/tests/api.spec.ts, e2e/tests/audio.spec.ts, e2e/tests/explorer.spec.ts, e2e/DEPRECATED.md</files>
  <read_first>e2e/DEPRECATED.md, frontend/package.json, frontend/playwright.config.ts, AGENTS.md</read_first>
  <acceptance_criteria>
    - `git ls-files "e2e/*"` returns only `e2e/DEPRECATED.md`.
    - `e2e/DEPRECATED.md` states that executable root-stack files were removed and `frontend/e2e/` is canonical.
    - No root `e2e/tests/*.spec.ts`, `e2e/page-objects/*.ts`, or root Playwright package/config files remain.
  </acceptance_criteria>
  <action>Delete the stale root `e2e/` implementation files now that Phase 06 established `frontend/e2e/` as the single active Playwright stack. Remove `e2e/package.json`, `e2e/package-lock.json`, `e2e/playwright.config.ts`, `e2e/run-tests.sh`, `e2e/README.md`, `e2e/data/hierarchy.json`, all files under `e2e/page-objects/`, and all files under `e2e/tests/`. Keep `e2e/DEPRECATED.md`, and update it to explicitly say the executable stack was removed in Phase 09 and that `frontend/e2e/` is the only active E2E surface.</action>
  <verify>
    <automated>$files = git ls-files "e2e/*"; if (($files | Where-Object { $_ -ne "e2e/DEPRECATED.md" })) { Write-Error "Unexpected tracked files remain under e2e/`n$files"; exit 1 }; rg -n "Phase 09|frontend/e2e/|only active" "e2e/DEPRECATED.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>The duplicate root E2E stack is reduced to a non-executable tombstone note only.</done>
</task>

<task type="auto">
  <name>Task 3: Harden ignore rules for the cleaned artifact classes</name>
  <files>.gitignore</files>
  <read_first>.gitignore, .planning/phases/09-safe-cleanup-repo-hygiene/09-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `.gitignore` contains explicit patterns for `frontend/coverage/`, `e2e/playwright-report/`, and `e2e/test-results/`.
    - Existing sensitive-file ignore rules remain intact.
    - No broad negation rule re-allows the removed generated artifacts.
  </acceptance_criteria>
  <action>Update `.gitignore` so the cleaned artifact classes stay untracked even in nested workspaces. Add explicit ignore entries for `frontend/coverage/`, `e2e/playwright-report/`, and `e2e/test-results/` while preserving the existing secret-file and env-file protections. Do not weaken the current `serviceAccountKey*.json`, `.env`, or `*.log` protections.</action>
  <verify>
    <automated>rg -n "frontend/coverage/|e2e/playwright-report/|e2e/test-results/|serviceAccountKey\*\.json|\.env" ".gitignore"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>Ignore rules match the deleted generated artifacts and preserve existing secret protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo->developer workflows | Generated outputs or duplicate test stacks can be mistaken for active sources |
| docs->test runners | Deprecated root E2E files can reintroduce wrong commands and conflicting behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | T | root `e2e/` implementation files | mitigate | Delete duplicate executable files and keep only a tombstone note pointing to `frontend/e2e/` |
| T-09-06 | D | tracked coverage/report artifacts | mitigate | Remove generated outputs and add explicit ignore patterns so git and tooling stay bounded |
| T-09-07 | I | debug logs and report bundles | mitigate | Delete committed logs/reports that may expose runtime data or screenshots |
| T-09-08 | R | cleanup provenance for deprecated stack | mitigate | Preserve `e2e/DEPRECATED.md` with an updated rationale instead of silently removing the whole directory |
</threat_model>

<verification>
- Prove generated artifact paths are gone from `git ls-files`.
- Prove root `e2e/` only retains `DEPRECATED.md`.
- Prove `.gitignore` has explicit nested patterns for the removed artifact classes.
</verification>

<success_criteria>
- No tracked generated report/log artifacts remain.
- The root E2E stack no longer contains executable duplicate files.
- Ignore rules prevent the cleaned artifact classes from being recommitted.
</success_criteria>

<output>
After completion, create `.planning/phases/09-safe-cleanup-repo-hygiene/09-02-SUMMARY.md`
</output>
