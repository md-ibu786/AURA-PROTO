---
phase: 06-verification-recovery
plan: 05
type: execute
wave: 3
depends_on:
  - 06-04
files_modified:
  - frontend/e2e/page-objects/index.ts
  - AGENTS.md
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "AGENTS.md documents frontend/e2e/ as the canonical E2E test location"
    - "Page objects have a single export point via index.ts"
    - "Test running instructions are clear and accurate"
  artifacts:
    - path: "frontend/e2e/page-objects/index.ts"
      provides: "Barrel export for all page objects"
      contains: "export"
    - path: "AGENTS.md"
      provides: "Updated E2E documentation"
      contains: "frontend/e2e/"
  key_links:
    - from: "AGENTS.md"
      to: "frontend/e2e/"
      via: "documentation reference"
      pattern: "E2E.*frontend/e2e"
---

<objective>
Update project documentation to reflect the single active verification stack.

Purpose: TEST-04 requires documentation to reflect the current product surface with no conflicting or stale test configuration defaults. After consolidation, AGENTS.md must point to the correct E2E location.
Output: Updated AGENTS.md with accurate E2E test instructions, page-objects barrel export.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-verification-recovery/06-04-SUMMARY.md

<interfaces>
<!-- Current AGENTS.md E2E section (lines 47-57) -->
```markdown
### E2E Tests (from `e2e/`)
npm test              # Run all E2E tests
npm run test:api      # API endpoint tests only
npm run test:ui       # UI interaction tests only
npm run test:audio    # Audio processing tests only
npm run show-report   # View HTML test report
```

<!-- Also in Project Structure section -->
```
├── e2e/                   # Playwright E2E tests (root level)
```

<!-- This is STALE - active stack is frontend/e2e/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create page-objects barrel export</name>
  <files>frontend/e2e/page-objects/index.ts</files>
  <read_first>frontend/e2e/page-objects/ExplorerPage.ts, frontend/e2e/page-objects/ApiHelper.ts</read_first>
  <action>
Create `frontend/e2e/page-objects/index.ts` as a barrel export for all page objects:

```typescript
/**
 * ============================================================================
 * FILE: index.ts
 * LOCATION: frontend/e2e/page-objects/index.ts
 * ============================================================================
 *
 * PURPOSE:
 *    Barrel export for E2E page objects
 *
 * ROLE IN PROJECT:
 *    Single import point for all page objects used in E2E tests.
 *
 * USAGE:
 *    import { ExplorerPage, ApiHelper } from './page-objects';
 * ============================================================================
 */

export { ExplorerPage } from './ExplorerPage';
export { ApiHelper } from './ApiHelper';
```

This allows tests to import all page objects from a single location.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit e2e/page-objects/index.ts 2>&1 | grep -E "error|Error" || echo "index.ts compiles"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/e2e/page-objects/index.ts` exists
    - File exports ExplorerPage and ApiHelper
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Page objects barrel export created</done>
</task>

<task type="auto">
  <name>Task 2: Update AGENTS.md E2E documentation</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md</read_first>
  <action>
Update AGENTS.md to reflect the consolidated E2E test stack:

1. **Update the E2E section** (currently titled "E2E Tests (from `e2e/`)"):

Replace with:
```markdown
### E2E Tests (from `frontend/`)
```bash
npm run test:e2e              # Run all E2E tests
npm run test:e2e:ui           # Run E2E tests with Playwright UI
npm run test:e2e:headed       # Run E2E tests with visible browser
```

**Run a single E2E test:**
```bash
npx playwright test e2e/explorer.spec.ts
npx playwright test --grep "test name pattern"
npx playwright test --debug  # Debug mode
```

> **Note:** The root-level `e2e/` directory is deprecated. All E2E tests live in `frontend/e2e/`.
```

2. **Update the Project Structure section** to clarify E2E locations:

Change:
```
├── e2e/                   # Playwright E2E tests (root level)
```

To:
```
├── e2e/                   # DEPRECATED - see frontend/e2e/
```

And ensure the frontend section shows:
```
│   ├── e2e/              # Playwright E2E tests (active stack)
```

3. **Update "Where to Look" table** if it references root e2e/:

Change any `e2e/*.spec.ts` to `frontend/e2e/*.spec.ts`.
  </action>
  <verify>
    <automated>grep -E "E2E tests.*frontend/e2e|DEPRECATED.*e2e" AGENTS.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md E2E section references `frontend/e2e/` as active location
    - AGENTS.md Project Structure marks root `e2e/` as deprecated
    - Test commands use frontend npm scripts (test:e2e, not e2e/ scripts)
    - "Where to Look" table points to frontend/e2e/ for E2E tests
  </acceptance_criteria>
  <done>AGENTS.md updated with correct E2E test documentation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan updates documentation only, not code.

| Boundary | Description |
|----------|-------------|
| None | Documentation changes have no runtime security implications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | N/A | Documentation | accept | No code affected |
</threat_model>

<verification>
```bash
# Page objects index compiles
cd frontend && npx tsc --noEmit e2e/page-objects/index.ts

# AGENTS.md references correct E2E location
grep -c "frontend/e2e" AGENTS.md  # Should be > 0
grep -c "DEPRECATED" AGENTS.md    # Should reference deprecated e2e/
```
</verification>

<success_criteria>
- [ ] `frontend/e2e/page-objects/index.ts` exists with barrel exports
- [ ] AGENTS.md E2E section points to `frontend/e2e/` as active stack
- [ ] AGENTS.md marks root `e2e/` as deprecated in project structure
- [ ] Test running commands in AGENTS.md use frontend npm scripts
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-recovery/06-05-SUMMARY.md`
</output>
