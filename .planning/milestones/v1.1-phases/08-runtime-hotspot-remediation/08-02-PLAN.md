---
phase: 08-runtime-hotspot-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/audio_processing.py
  - api/tests/test_audio_processing_job_store.py
  - frontend/src/components/explorer/UploadDialog.tsx
  - frontend/src/components/explorer/__tests__/UploadDialog.test.tsx
  - frontend/src/features/kg/hooks/useKGProcessing.test.tsx
autonomous: true
requirements:
  - PERF-02
  - PERF-04

must_haves:
  truths:
    - "Completed and failed audio jobs do not accumulate forever in process memory"
    - "Active audio jobs remain visible while running, but terminal jobs expire or are evicted predictably"
    - "Closing or unmounting the upload dialog stops polling timers and cancels owned in-flight requests safely"
    - "KG queue polling stops when there are no processing items left"
  artifacts:
    - path: "api/audio_processing.py"
      provides: "Bounded job status retention and cleanup behavior"
      contains: "job_status_store"
    - path: "api/tests/test_audio_processing_job_store.py"
      provides: "Regression tests for job status eviction and TTL behavior"
      exports: ["test_completed_jobs_are_pruned", "test_active_jobs_are_not_evicted"]
    - path: "frontend/src/components/explorer/__tests__/UploadDialog.test.tsx"
      provides: "Regression tests for dialog polling and abort cleanup"
      exports: ["test_clears_polling_on_close", "test_aborts_requests_on_unmount"]
    - path: "frontend/src/features/kg/hooks/useKGProcessing.test.tsx"
      provides: "Regression coverage for queue polling stop behavior"
      contains: "refetchInterval"
  key_links:
    - from: "api/audio_processing.py"
      to: "GET /api/audio/pipeline-status/{job_id}"
      via: "bounded job_status_store lifecycle"
      pattern: "pipeline-status"
    - from: "frontend/src/components/explorer/UploadDialog.tsx"
      to: "/api/audio/pipeline-status/"
      via: "pollStatus fetch with cleanup"
      pattern: "AbortController|clearInterval"
---

<objective>
Bound the audio pipeline's runtime job-state memory and lock in safe frontend polling/upload cleanup with regression coverage.

Purpose: The audio pipeline currently leaks terminal job state across long-running sessions, and the frontend's cleanup behavior is verified only by inspection. This plan makes the backend retention bounded and proves the frontend lifecycle cleanup with tests.

Output: Bounded `job_status_store` behavior in `api/audio_processing.py`, backend eviction tests, and frontend cleanup tests plus explicit abort handling where needed.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-runtime-hotspot-remediation/08-RESEARCH.md

# Key source files
@api/audio_processing.py
@frontend/src/components/explorer/UploadDialog.tsx
@frontend/src/features/kg/hooks/useKGProcessing.ts
@frontend/src/features/kg/hooks/useKGProcessing.test.tsx
</context>

<interfaces>
From api/audio_processing.py:
```python
job_status_store[job_id] = {
    'status': 'pending' | 'transcribing' | 'refining' | 'summarizing' | 'complete' | 'error',
    'progress': int,
    'message': str,
    'result': dict | None,
}
```

From frontend/src/features/kg/hooks/useKGProcessing.ts:
```typescript
refetchInterval: (query) => {
    const queue = query.state.data as Array<{ status: string }> | undefined;
    const hasActiveItems = queue?.some(item => item.status === 'processing');
    return hasActiveItems ? 2000 : false;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add backend tests for bounded job status retention</name>
  <files>api/tests/test_audio_processing_job_store.py</files>
  <behavior>
    - Test 1: terminal jobs older than the configured TTL are pruned before new writes or status reads.
    - Test 2: when the store exceeds the max-entry cap, the oldest terminal jobs are evicted first.
    - Test 3: active jobs (`pending`, `transcribing`, `refining`, `summarizing`) are retained and are not evicted ahead of terminal jobs.
    - Test 4: `pipeline-status` returns not-found behavior for jobs that were legitimately expired or evicted.
  </behavior>
  <action>
Create `api/tests/test_audio_processing_job_store.py` with focused unit coverage for the retention helpers that will be added to `api/audio_processing.py`.

Implementation guidance for the test file:
1. Import the cleanup helpers and `job_status_store` from `api.audio_processing`.
2. Freeze or stub time so TTL-based pruning is deterministic.
3. Seed the store with a mix of active and terminal jobs carrying explicit timestamps.
4. Assert eviction order and not-found behavior through the public status-read path where practical.
  </action>
  <verify>
    <automated>pytest api/tests/test_audio_processing_job_store.py -q</automated>
  </verify>
  <done>Backend retention tests exist and fail before implementation because the current store has no TTL or max-entry behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Bound `job_status_store` with TTL and terminal-entry eviction</name>
  <files>api/audio_processing.py</files>
  <action>
Modify `api/audio_processing.py` to make `job_status_store` bounded without changing the existing API contract for active jobs:

1. Add explicit retention constants near the store definition (for example `JOB_STATUS_TTL_SECONDS` and `JOB_STATUS_MAX_ENTRIES`).
2. Add helper functions that:
   - stamp entries with update timestamps,
   - prune terminal jobs older than the TTL,
   - evict oldest terminal jobs when the store exceeds the cap,
   - preserve active in-flight jobs until they become terminal.
3. Call cleanup before inserting/updating job state and before serving `GET /api/audio/pipeline-status/{job_id}`.
4. When a job has legitimately expired/been evicted, return the existing not-found response path instead of recreating or leaking stale state.
5. Keep the implementation local to `audio_processing.py`; do not add a new infrastructure dependency for this brownfield remediation.
  </action>
  <verify>
    <automated>pytest api/tests/test_audio_processing_job_store.py -q</automated>
  </verify>
  <done>The audio pipeline store is bounded by TTL and max entries, active jobs are preserved while running, and backend retention tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Lock in frontend polling cleanup and abort in-flight requests safely</name>
  <files>frontend/src/components/explorer/UploadDialog.tsx, frontend/src/components/explorer/__tests__/UploadDialog.test.tsx, frontend/src/features/kg/hooks/useKGProcessing.test.tsx</files>
  <action>
Add regression coverage for PERF-04 and harden cleanup behavior where needed:

1. Create `frontend/src/components/explorer/__tests__/UploadDialog.test.tsx` to verify that:
   - polling intervals are cleared on dialog close,
   - polling intervals are cleared on unmount,
   - owned in-flight upload/status requests are aborted or safely ignored during teardown.
2. Update `frontend/src/components/explorer/UploadDialog.tsx` to use `AbortController` for the upload and pipeline-status fetches if the tests expose that requests can outlive the dialog lifecycle. Ignore `AbortError` intentionally instead of surfacing a user-facing failure on normal teardown.
3. Extend `frontend/src/features/kg/hooks/useKGProcessing.test.tsx` to assert that the queue polling interval resolves to `false` when there are no items with `status === 'processing'`.

Do not rewrite the KG hook architecture. This task is a regression lock and targeted cleanup-hardening pass only.
  </action>
  <verify>
    <automated>cd frontend && npm test -- src/components/explorer/__tests__/UploadDialog.test.tsx src/features/kg/hooks/useKGProcessing.test.tsx --run</automated>
  </verify>
  <done>Frontend cleanup behavior is covered by tests, UploadDialog stops timers and cancels owned requests on teardown, and queue polling stops once processing is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→API | Upload dialog owns timers and requests that must be cleaned up when UI ownership ends |
| API→Process memory | In-memory job state must not grow without bound across long-running sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | `job_status_store` in `api/audio_processing.py` | mitigate | Add TTL cleanup and max-entry eviction that targets terminal jobs first |
| T-08-05 | Denial of Service | `UploadDialog.tsx` polling/request lifecycle | mitigate | Clear timers on close/unmount and abort owned fetches with `AbortController` |
| T-08-06 | Repudiation | Expired job status lookups | accept | Expired jobs intentionally return not-found after bounded retention; this is acceptable because the store is transient runtime state |
</threat_model>

<verification>
1. Run `pytest api/tests/test_audio_processing_job_store.py -q`.
2. Run `cd frontend && npm test -- src/components/explorer/__tests__/UploadDialog.test.tsx src/features/kg/hooks/useKGProcessing.test.tsx --run`.
3. Verify retention constants and cleanup helpers exist: `rg "JOB_STATUS_(TTL|MAX)" api/audio_processing.py`.
</verification>

<success_criteria>
- [ ] Terminal audio jobs expire or are evicted instead of accumulating indefinitely
- [ ] Active audio jobs remain readable while running
- [ ] `GET /api/audio/pipeline-status/{job_id}` returns not-found for legitimately expired jobs
- [ ] Upload dialog clears polling timers on close/unmount
- [ ] Owned in-flight upload/status requests are aborted or safely ignored during teardown
- [ ] `useKGProcessing` polling stops when no queue items remain in processing state
</success_criteria>

<output>
After completion, create `.planning/phases/08-runtime-hotspot-remediation/08-02-SUMMARY.md`
</output>
