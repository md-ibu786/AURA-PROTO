---
phase: 06-testing-verification
plan: 06-01
type: execute
---

<objective>
Verify the complete mobile responsive implementation across target devices using Chrome DevTools emulation.

Purpose: All responsive CSS and React changes have been implemented in Phases 1-5. This phase verifies the end-to-end mobile experience works correctly — no horizontal scroll, proper navigation flow, readable content, and usable dialogs.
Output: Verification report documenting test results across iPhone SE, iPhone 12, and iPad Air.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/mobile-responsive/ROADMAP.md
@frontend/MOBILE_RESPONSIVE_PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify build and type check pass</name>
  <files>frontend/src/styles/explorer.css, frontend/src/components/layout/Header.tsx, frontend/src/components/layout/Sidebar.tsx, frontend/src/stores/useExplorerStore.ts</files>
  <action>
  Run comprehensive verification commands:

  1. `cd frontend && npx tsc --noEmit` — type check all TypeScript
  2. `cd frontend && npm run build` — full production build
  3. `cd frontend && npm run lint` — ESLint check

  All three must pass cleanly with zero errors. If any fail, fix the issues before proceeding to the visual verification checkpoint.
  </action>
  <verify>All three commands exit with code 0</verify>
  <done>Type check, build, and lint all pass cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Complete mobile responsive implementation across the AURA-NOTES-MANAGER frontend:
  - Sidebar transforms to slide-in overlay on mobile with backdrop and close button
  - Header gains hamburger menu button and truncated breadcrumbs
  - Search bar collapses to icon-toggle on mobile
  - ListView hides auxiliary columns on mobile
  - GridView uses compact sizing with 3-column layout
  - SelectionActionBar uses icon-only buttons on mobile
  - All dialogs capped at 90vw with full-width buttons
  </what-built>
  <how-to-verify>
  1. Start the dev server: `cd frontend && npm run dev`
  2. Open Chrome DevTools (F12) → Toggle Device Toolbar (Ctrl+Shift+M)

  **Test on iPhone SE (375×667):**
  - [ ] No horizontal scrollbar
  - [ ] Sidebar is hidden
  - [ ] Hamburger button visible in header
  - [ ] Click hamburger → sidebar slides in from left
  - [ ] Click backdrop → sidebar closes
  - [ ] Click tree item → sidebar closes, content loads
  - [ ] Breadcrumbs show: Home > ... > Current
  - [ ] Click search icon → search bar expands
  - [ ] GridView shows 3 columns with compact icons
  - [ ] Switch to ListView → only icon + name visible
  - [ ] Select items → action bar shows icon-only buttons

  **Test on iPhone 12 (390×844):**
  - [ ] Same checks as iPhone SE
  - [ ] All content readable and properly spaced

  **Test on iPad Air (820×1180):**
  - [ ] Portrait: sidebar hidden, hamburger visible
  - [ ] Landscape (toggle rotation): sidebar may show — verify layout works

  **Test dialogs:**
  - [ ] Right-click (long press) → context menu fits within screen
  - [ ] Trigger delete → confirm dialog fits within screen
  - [ ] Upload dialog fits properly (if at module level)
  </how-to-verify>
  <resume-signal>Type "approved" to finalize, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build passes
- [ ] Lint passes
- [ ] Type check passes
- [ ] Human verification approved
</verification>

<success_criteria>
- Zero horizontal scroll on all tested devices
- Complete navigation flow works: hamburger → sidebar → item → close → content
- All dialogs usable on mobile
- Desktop layout completely unaffected
- Human tester approves all checkpoints
</success_criteria>

<output>
After completion, create `.planning/mobile-responsive/phases/06-testing-verification/06-01-SUMMARY.md`
</output>
