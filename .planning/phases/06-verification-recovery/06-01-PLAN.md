---
phase: 06-verification-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/e2e/fixtures.ts
  - frontend/e2e/auth.spec.ts
  - frontend/e2e/rbac.spec.ts
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Auth and RBAC E2E tests can import useMockAuth from fixtures without TypeScript errors"
    - "Tests use the correct mock auth detection function"
  artifacts:
    - path: "frontend/e2e/fixtures.ts"
      provides: "useMockAuth export alias for isMockAuthEnabled"
      contains: "export { isMockAuthEnabled as useMockAuth }"
  key_links:
    - from: "frontend/e2e/auth.spec.ts"
      to: "frontend/e2e/fixtures.ts"
      via: "named import"
      pattern: "import.*useMockAuth.*from.*fixtures"
---

<objective>
Fix broken fixture imports in frontend E2E auth and RBAC test suites.

Purpose: Auth and RBAC tests currently fail to compile due to missing `useMockAuth` export from fixtures. This blocks TEST-01 requirement.
Output: Working imports, no TypeScript errors in E2E test files.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-verification-recovery/06-RESEARCH.md

<interfaces>
<!-- Key exports from fixtures.ts that tests depend on -->

From frontend/e2e/fixtures.ts (current):
```typescript
export function isMockAuthEnabled(): boolean {
    return process.env.VITE_USE_MOCK_AUTH === 'true';
}
// NOTE: useMockAuth is NOT exported but is used in auth.spec.ts and rbac.spec.ts
```

From frontend/e2e/auth.spec.ts (line 41-42):
```typescript
import {
    useMockAuth,  // ERROR: Module '"./fixtures"' has no exported member 'useMockAuth'
} from './fixtures';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useMockAuth export alias to fixtures.ts</name>
  <files>frontend/e2e/fixtures.ts</files>
  <read_first>frontend/e2e/fixtures.ts, frontend/e2e/auth.spec.ts</read_first>
  <action>
Add a named export alias for the existing `isMockAuthEnabled` function. At the end of the file (around line 805), add:

```typescript
// Alias for backward compatibility with test files
export { isMockAuthEnabled as useMockAuth };
```

This provides the `useMockAuth` name that `auth.spec.ts` and `rbac.spec.ts` expect while preserving the existing `isMockAuthEnabled` function.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit e2e/fixtures.ts 2>&1 | grep -E "error|Error" || echo "No TypeScript errors"</automated>
  </verify>
  <acceptance_criteria>
    - fixtures.ts contains `export { isMockAuthEnabled as useMockAuth }`
    - No TypeScript compilation errors in fixtures.ts
  </acceptance_criteria>
  <done>The useMockAuth export exists in fixtures.ts and compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Verify auth.spec.ts compiles after fixture fix</name>
  <files>frontend/e2e/auth.spec.ts</files>
  <read_first>frontend/e2e/auth.spec.ts</read_first>
  <action>
Verify that auth.spec.ts now compiles without the "Module has no exported member 'useMockAuth'" error. 

No code changes needed if the fixture export was added correctly. If there are other import issues, fix them:
- Ensure all imports from './fixtures' match actual exports
- The expected imports are: test, expect, loginAsRole, clearAuth, isAuthenticated, getCurrentUser, useMockAuth
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit e2e/auth.spec.ts 2>&1 | grep -E "error|Error" || echo "No TypeScript errors in auth.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit e2e/auth.spec.ts` produces no errors
    - Import statement `import { useMockAuth } from './fixtures'` resolves correctly
  </acceptance_criteria>
  <done>auth.spec.ts compiles without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 3: Verify rbac.spec.ts compiles after fixture fix</name>
  <files>frontend/e2e/rbac.spec.ts</files>
  <read_first>frontend/e2e/rbac.spec.ts</read_first>
  <action>
Verify that rbac.spec.ts now compiles without the "Module has no exported member 'useMockAuth'" error.

No code changes needed if the fixture export was added correctly. If there are other import issues, fix them:
- Ensure all imports from './fixtures' match actual exports
- The expected imports include: test, expect, mockTreeResponse, mockCrudResponses, waitForLoading, loginAsRole, clearAuth, useMockAuth, waitForAuth, isAuthenticated
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit e2e/rbac.spec.ts 2>&1 | grep -E "error|Error" || echo "No TypeScript errors in rbac.spec.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit e2e/rbac.spec.ts` produces no errors
    - Import statement `import { useMockAuth } from './fixtures'` resolves correctly
  </acceptance_criteria>
  <done>rbac.spec.ts compiles without TypeScript errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies test infrastructure only, not production code.

| Boundary | Description |
|----------|-------------|
| None | Test code does not handle untrusted input in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Test credentials in fixtures | accept | Credentials are mock/test values, not real secrets. File is in e2e test directory, not deployed. |
</threat_model>

<verification>
```bash
# All E2E TypeScript files compile without errors
cd frontend && npx tsc --noEmit e2e/*.ts e2e/*.spec.ts
```
</verification>

<success_criteria>
- [ ] `frontend/e2e/fixtures.ts` exports `useMockAuth`
- [ ] `frontend/e2e/auth.spec.ts` compiles without TypeScript errors
- [ ] `frontend/e2e/rbac.spec.ts` compiles without TypeScript errors
- [ ] All three files type-check successfully with `npx tsc --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-recovery/06-01-SUMMARY.md`
</output>
